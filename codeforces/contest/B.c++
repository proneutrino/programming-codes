#include<bits/stdc++.h>
using namespace std;
int main()
{
	int m,n,i,j,c;
	char a[1000][1000];
	cin >> m >> n;
	for (i = 0 ; i < m ; i++)
		cin >> a[i];
	for (i = 0 ; i < m ; i++)
	{
		for (j = i+1 ; j < n ; j++)
		{
			if(a[i] == a[j])
				c += 1;
		}
		if (c == n)
		{
			k[h++] = ;
			c = 0;
		}
		c = 0;
		i = j;
	}
	if (h != 3)
	{
		for (j = 0 ; j < n ; j++)


	
}
